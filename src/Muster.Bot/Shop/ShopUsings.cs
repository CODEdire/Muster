// Self-export: every feature globally publishes its own namespace from its own folder so the rest of
// the project (Program.cs, sibling features) can resolve its types without explicit using directives.
global using Muster.Bot.Shop;
global using Muster.Bot.Shop.Modules;
